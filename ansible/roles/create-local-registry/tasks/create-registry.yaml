---

- name: Install docker registry
  yum:
    name: docker-distribution
  tags: prepare

- name: Config registry
  template:
    src: ../templates/config.yaml.j2
    dest: /etc/docker-distribution/registry/config.yaml
    mode: 0644
    owner: root
    group: root
  tags: prepare

- name: Config accept port 8787
  iptables:
    chain: INPUT
    destination_port: 8787
    jump: ACCEPT
  become: yes
  tags: prepare

- name: Start docker-distribution
  systemd:
    name: docker-distribution
    state: started
    enabled: true
  tags: prepare
